期末試験問題1: ファイルをコピーせよ

この環境には、ファイル`exam`(`~/exam`)が置かれている。

# 設問1

ファイル `exam` をディレクトリ `dest` 以下にコピーせよ。ファイル名はそのままでよい。

# 設問2

ファイル `exam` をカレントディレクトリ上に `result` という名前でコピーせよ

# 設問3

ファイル `exam` をディレクトリ `/usr/local/lib` 以下にコピーせよ(ファイル名は`exam`のままでよい)。
ただしこのディレクトリは一般ユーザーの権限ではエラーが出るため、よく考えて操作すること
