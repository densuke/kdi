期末試験問題2: ディレクトリを作成せよ

以下の設問に沿ったディレクトリの操作をせよ、基準はホームディレクトリ(開始直後のディレクトリ)とする。

# 設問1

ディレクトリ `e1` を作成せよ。

# 設問2

現時点で存在するディレクトリ `hoge` を `e2` にリネームせよ(中身には触れないこと)

# 設問3

現時点で存在するディレクトリ `fuga` には、中に既にディレクトリ`e3`が存在する。
この`e3`ディレクトリをホームディレクトリ上に移動させ、代わりに`fuga`ディレクトリ中に
`ok`ディレクトリを作成せよ。

(before)              (after)

.                   .
└── fuga       ->   ├── e3
    └── e3          └── fuga
                        └── ok
