(再追試)期末試験問題2: ディレクトリを作成せよ

以下の設問に沿ったディレクトリの操作をせよ、基準はホームディレクトリ(開始直後のディレクトリ)とする。

# 設問1

ディレクトリ `ee1` を作成せよ。

# 設問2

現時点で存在するディレクトリ `hoge` を `ee2` にリネームせよ(中身には触れないこと)

# 設問3

現時点で存在するディレクトリ `fuga` には、中に既にディレクトリ`ee3`が存在する。
この`ee3`ディレクトリをホームディレクトリ上に移動させ、代わりに`fuga`ディレクトリ中に
`ok`ディレクトリを作成せよ。

(before)              (after)

.                   .
└── fuga       ->   ├── ee3
    └── ee3         └── fuga
                        └── ok
