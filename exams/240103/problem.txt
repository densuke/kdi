期末試験問題3: ファイル操作(1)

以下の設問に沿ったファイル操作をせよ。
特に指示がなければカレントディレクトリ(ホームディレクトリ)上で作成すること。
なお、この問題においては`vi(vim)`コマンドは封鎖しているので注意せよ。

# 設問1

空ファイル `newfile` を作成せよ。

# 設問2

既存のファイル `delfile`(中身あり)を削除せよ。

# 設問3

ファイル `textfile` を作成せよ。
中身は `fugahoge` とし、末尾に改行コード(LF)を入れておくこと。

# 設問4

既存のファイル `modfile` のファイルサイズを0にせよ。
ヒント: リダイレクトの種類によっては…
