問題: ディレクトリはどれか?

この練習環境には、多数のファイルの中にディレクトリが1つ隠れている。
そのディレクトリを見つけ出し、answerコマンドでディレクトリ名を出力せよ。
(見つける方法は`ls`コマンドでしょう)

例えばhogeがディレクトリだった場合

```
$ answer "hoge"
%%%:hoge:%%%
```

として出力していればそれが評価に使われる。
※ 複数回実行した場合は最後のものが採用されます
