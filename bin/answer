#!/bin/bash

# 引数を回答としてマーカーを付けて出力するコマンドです。
# 引数が無い場合、ヘルプを出します。
# 回答としては "%%%:回答:%%%" のフォーマットで出力します。

if [ $# -eq 0 ]; then
    echo "Usage: $0 answer"
    echo "問題の回答としてマーカー付き出力を行います。"
    exit 1
fi

echo "%%%:$1:%%%"
